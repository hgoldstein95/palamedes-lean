let rec gen_term_size = fun num_arr_tau ->
  fun num ->
    fun gamma ->
      fun tau ->
        let (xccc0) = sizecheck num in
        match xccc0 with
        | True ->
            let (_x3) = gamma in
            let (_x20) = gen_term_no_app _x3 in let (_x4) = tau in _x20 _x4
        | False ->
            let (xccc1) = bool_gen () in
            (match xccc1 with
             | True ->
                 let (arg_tau) = gen_type () in
                 (match true with
                  | True ->
                      let (xccc2) = int_range_inex 0 in
                      let (num_app_func) = xccc2 num in
                      let (xccc3) = num - num_app_func in
                      let (num_app_arg) = subs xccc3 in
                      let (func_ty) = Stlc_ty_arr (arg_tau, tau) in
                      let (num_arr_func_ty) = get_num_arr func_ty in
                      let (xccc4) = gen_term_size num_arr_func_ty in
                      let (xccc5) = xccc4 num_app_func in
                      let (xccc6) = xccc5 gamma in
                      let (xccc7) = Stlc_ty_arr (arg_tau, tau) in
                      let (func) = xccc6 xccc7 in
                      let (num_arr_arg_ty) = get_num_arr arg_tau in
                      let (xccc8) = gen_term_size num_arr_arg_ty in
                      let (xccc9) = xccc8 num_app_arg in
                      let (xccc10) = xccc9 gamma in
                      let (arg) = xccc10 arg_tau in Stlc_app (func, arg)
                  | False -> Err)
             | False ->
                 (match tau with
                  | Stlc_ty_nat -> Err
                  | Stlc_ty_arr (tau1, tau2) ->
                      let (num_arr_tau2) = get_num_arr tau2 in
                      let (xccc11) = gen_term_size num_arr_tau2 in
                      let (xccc12) = xccc11 num in
                      let (xccc13) = Stlc_tyctx_cons (tau1, gamma) in
                      let (xccc14) = xccc12 xccc13 in
                      let (body) = xccc14 tau2 in Stlc_abs (tau1, body)))